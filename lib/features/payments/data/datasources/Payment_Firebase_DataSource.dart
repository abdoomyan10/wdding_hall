// features/payment/data/datasources/payment_firebase_data_source.dart
class PaymentFirebaseDataSource {
  Future<void> updatePayment(String paymentId, Map<String, dynamic> data) async {
    // تنفيذ تحديث الدفع في Firebase
    // مثال: await FirebaseFirestore.instance.collection('payments').doc(paymentId).update(data);
  }
}